#include <stdio.h>
#include <stdlib.h>

/*Implementar un programa en C que permita gestionar una lista de sensores en una fábrica.
Cada sensor tendrá una estructura con la siguiente información:
ID (entero)
Tipo (entero: 1 para temperatura, 2 para humedad)
Valor medido (float)

El programa debe incluir al menos las siguientes funciones:
void agregarSensor(Sensor sensores[], int *numSensores);
void mostrarSensores(Sensor sensores[], int numSensores);
Requisitos:
Utilizar un vector de estructuras para almacenar la información de los sensores.
Las funciones deben tener su propio archivo fuente (.c) y archivo de cabecera (.h).
Manejar adecuadamente el alcance de las variables.

Definir una constante para el número máximo de sensores.
La función agregarSensor debe permitir agregar un nuevo sensor al vector.
La función mostrarSensores debe imprimir la lista de sensores en la consola.*/

int main()
{

    Sensor sensores [maxVectores];} // vector de tipo estructura con tamaño maxVectores
    printf("Hello world!\n");
    return 0;
}
